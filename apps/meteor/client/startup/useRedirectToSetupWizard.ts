export const useRedirectToSetupWizard = (): void => {
	// Setup wizard redirect disabled - enterprise customization
	// Always skip setup wizard regardless of settings to prevent registration flow
};
